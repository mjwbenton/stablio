extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.0"
    import: ["@key", "@shareable"]
  )

type Query {
  book(id: ID!): Book
}

type Book {
  highlights: [Highlight!]!
}

type Highlight {
  text: String!
  location: Int!
}
