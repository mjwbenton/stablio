overwrite: true
schema:
  - "./schema.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      skipTypename: true
      useIndexSignature: true
